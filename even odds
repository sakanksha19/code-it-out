#include <iostream>
using namespace std;

int main() {
	long long int n,k,c;
	cin>>n>>k;
	if(n%2==0)
	{
		if(k<=n/2)
		{
			c=2*k-1;
		}
		else
		{
			c=2*(k-n/2);
		}
	}
	else
	{
		if(k<=(n/2)+1)
		{
			c=2*k-1;
		}
		else
		{
			c=2*(k-((n/2)+1));
		}
	}
	cout<<c;
	return 0;
}
